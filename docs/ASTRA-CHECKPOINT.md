# Decision Engine checkpoint

## CURRENT COMMIT

Entry: `f0ed51aa4f487b88f2a90b27fa7392e9d428ec27`, clean worktree, local matches remote.

## COMPLETED

Independent OR/statistical, match-data/score, and football/product adversarial council.
Read Stage 3 questions, registry/specs, providers, API, CI and licensing guard.
Decision claim: satisfy explicit tactical requirements with the least structural
compromise. No universal utility. Match Lab launches with a contribution vector.

## STATE MAP AT ENTRY

- VERIFIED COMPLETE: five executable season constructs, historical adapters,
  profile builder and Player Lab implementation. Local historical corpora present.
- PARTIALLY COMPLETE: Stage 2 release gates, statistical/browser verification.
- STALE DOCUMENTATION: ROADMAP Stage 1/1C/2 and LIVE physical feed; UEFA licensing table.
- UNVERIFIED: full local tests, actual rendered app, human acceptance.
- BROKEN: independently resampled player/construct draws lose shared-match covariance;
  gated comparison/scatter values leak; statistical significance called materiality.
- NOT STARTED: match intelligence/UI, XI solver/UI, opponent/transfer models.

## IN PROGRESS

- Root: shared bootstrap, compatibility, API gates, historical XI snapshots/API,
  integration, docs, tests, screenshot review.
- decision_council: `galactico/optimization/xi/`, `tests/test_xi*.py`.
- match_council: `galactico/match_lab/`, additive provider enrichment, match tests,
  match-score experiment/E-05 and capability matrix.
- adversarial_product: `web/match.html`, `web/xi.html`, `web/labs.css`, navigation,
  `e2e/labs.spec.js`.

## FAILED EXPERIMENTS

None executed yet. Scalar match utility is not identified; baseline audit pending.

## OPEN BLOCKERS

Initial sandbox startup stalled. User enabled full access; new tool calls must omit
`sandbox_permissions`. Workspace `C:\Users\Owner\galactico`.

## EXACT NEXT COMMANDS

`python -m pytest -q -m "not research"`; `python scripts/build_profiles.py` after
bootstrap repair; `python -m ruff check galactico tests`; `npx playwright test`;
`python scripts/check_licensing.py`. Stage only specific code/docs/test files.

## EXACT NEXT FILES

`galactico/profiles/uncertainty.py`, `galactico/profiles/build.py`,
`galactico/api/player_lab.py`, new historical snapshot/API modules; README,
ROADMAP, DECISIONS, VALIDATION, KNOWN_LIMITATIONS.

## AGENT RESULTS NOT YET INTEGRATED

Historical cutoff must filter xT training and player inputs. Width is pass-origin
share, not team width. Equivalent solutions can create fake stable cores. Wyscout
has no direct recipients/xG. Match totals are descriptions, not persistent ability.
